"use client";

/**
 * The TV detail page's cast shelf.
 *
 * Identical to the movie one — see @/components/ui/other/CastRail. This file
 * stays only because `src/app/tv/[id]/page.tsx` imports it by path.
 */
export { default } from "@/components/ui/other/CastRail";
